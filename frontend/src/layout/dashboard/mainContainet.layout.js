import React from 'react';

const MainContainetLayout = ({children}) => {
    return(
        <section className="mainContainetLayout">
            {children}
        </section>
    )
}

export default MainContainetLayout;